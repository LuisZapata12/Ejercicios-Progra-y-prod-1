
using System;


public class Herencia : Padre
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Console.WriteLine("herede de padre " + a);
        Console.WriteLine("herede de padre " + b);
    }
}
